# PoseScaleTomlGenarator
This tool reads the gm_module_tbl, obtains PoseScale data matching the module data within it, and converts these into gm_module_pose_tbl.toml (default value) and scale_db.toml.  
